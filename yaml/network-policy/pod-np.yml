apiVersion: apps/v1beta2
kind: Depolyment
metadata:
  name: nginx
  namespace: policy-demo
  labels:
    env: test
spec:
  containers:
  - name: nginx
    image: nginx
    imagePullPolicy: IfNotPresent
